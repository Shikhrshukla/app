pipeline{
    agent{
        docker { image 'python:3.9-slim'}
    }

    environment{
        DOCKER_IMAGE = 'shikhrshukla/app'
        DOCKERHUB_CREDENTIALS = credentials('dockerhub-credentials')
    }

    stages{
        
    }
}